import assert from 'assert';
import reduxReducerFactory from '../lib';

describe('redux-reducer-factory', function () {
  it('should have unit test!', function () {
    /*
      define test enhancers, with asserts built into them
      assert that bad enhancer arrays are validated
      assert that bad enhancers are validated

      invoke createReducerFactory with test enhancers

      assert that it's... a function?

      invoke createReducer with test state, handlers

      assert that handlerMap validation console.errors with bad handler
      assert that it's a function

      invoke reducer with test previous state and test action; assertive test enhancers should take it from there

      test default createReducer
    */


  });
});
